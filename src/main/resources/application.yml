spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/blog
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
  flyway:
    out-of-order: true

application:
  security:
    signature: ${SIGNATURE:blog-secret-very-very-very-secret-key-that-you-dont-know-aertthrtrwetethrewfg-dstdhgsdfa}
    lifetime: 1d